import { Carousel } from 'components';
import React, { Fragment } from 'react';
import Bot from './Bot';

const Home = () => {
	return (
		<Fragment>
			<Carousel />
			<Bot />
		</Fragment>
	);
};

export default Home;
